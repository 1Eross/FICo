{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: ENV",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      },
      "console": "internalConsole",
    }
  ]
}
